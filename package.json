{
  "name": "hunk-gh-stacked-pr",
  "version": "0.1.0",
  "description": "Hunk extension: full GitHub PR review — threads, checks, timeline, merge, stacks — without leaving the terminal",
  "license": "MIT",
  "repository": "github:proeschel/hunk-gh-stacked-pr",
  "hunk": {
    "extensions": ["./index.tsx"],
    "apiVersion": 6
  },
  "devDependencies": {
    "@opentui/core": "^0.5.4",
    "@opentui/react": "^0.5.4",
    "@types/node": "^24.0.0",
    "@types/react": "^19.2.18",
    "hunkdiff": "^0.19.0",
    "react": "^19.2.8",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  }
}
